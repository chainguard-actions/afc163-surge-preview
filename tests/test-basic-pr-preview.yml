name: Test basic PR preview
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/1/merge
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create dist folder with index.html
        run: |
          mkdir -p public
          echo "<html><body>Hello Surge Preview</body></html>" > public/index.html

      - id: action
        uses: .
        with:
          surge_token: '6973bdb764f0d5fd07c910de27e2d7d0'
          github_token: ${{ github.token }}
          build: 'echo "skip build"'
          dist: 'public'
        continue-on-error: true

      - name: Assert action ran
        run: |
          echo "Action completed (may fail due to network/token constraints in test env)"
          echo "preview_url: ${{ steps.action.outputs.preview_url }}"
          echo "PASS: action step executed without crashing the workflow"
